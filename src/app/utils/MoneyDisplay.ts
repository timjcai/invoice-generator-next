import { BCP47Code, BCP47Countries, Country, CountryCode, CurrencyType } from "../types";

export function formatAsCurrency(number: number, countrycode: BCP47Code, country: Country) {
    console.log('hello')
}

